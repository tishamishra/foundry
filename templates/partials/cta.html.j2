<section class="cta reveal" id="quote">
  <div class="wrap cta-in">
    <div>
      <h2>{{ content.cta.heading }}</h2>
      <p>{{ content.cta.text }}</p>
    </div>
    <a class="btn btn-accent btn-lg" href="tel:{{ t.phone_link }}">{{ content.cta.button }}</a>
  </div>
</section>
